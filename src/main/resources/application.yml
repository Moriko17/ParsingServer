server:
  port: 8817
spring:
  application:
    name: yummy-parser
  datasource:
    url: jdbc:postgresql://localhost:5432/animelist
    username: postgres
    password: root
  jpa:
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false